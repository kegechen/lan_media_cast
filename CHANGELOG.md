# Changelog

## 1.0.1 - 2026-09-02

### 新增

- Windows 发送端可通过已配对连接一键获取接收端最近的诊断日志。
- 接收端诊断日志保留在有界内存环中，包含播放和媒体网络错误，便于定位问题。
- 接收端证书变化时，发送端不再无休止重连，改为提示「接收端身份已变化」并提供重新信任入口；
  确认后清除旧 pin 与 token 并重新配对一次。

### 修复

- 浏览器 Cookie 读取失败不再导致整个网页视频解析失败。Chrome 与 Edge 自 127 起启用应用绑定
  加密，外部程序无法解密其 Cookie，而登录状态对多数页面本就是可选的；现在会自动改用未登录
  方式重试一次，并在状态栏说明「已按未登录解析」。
- Windows 下按目标主机决定是否使用系统代理，并注入 yt-dlp 子进程环境。CPython 的
  `getproxies()` 是 `getproxies_environment() or getproxies_registry()`，而 `no_proxy` 也匹配
  `_proxy` 后缀，因此只设了 `NO_PROXY` 的机器会得到一个非空字典，注册表里的系统代理**永远
  不会被读取**。旁路判断按 WinINET 语义在本地完成（支持 `10.*`、`*.corp.example`、`?`
  单字符通配和 `<local>`；裸条目按精确匹配，不隐含子域），不交给 `no_proxy`——后者只支持精确
  与后缀匹配，通配符条目会失效并把内网请求送进外部代理。用户自己的 `NO_PROXY` 会并入旁路
  列表（按 CPython 的后缀语义展开）。配置了 PAC 脚本时不使用手动代理，避免与浏览器走不同线路；
  `ProxyServer` 只声明 `http=` 时，HTTPS 用 yt-dlp 的 `__noproxy__` 哨兵保持直连——仅省略
  `HTTPS_PROXY` 是不够的，yt-dlp 会把 http 代理沿用到 https。
- 网页解析的降级提示不再写进状态栏。状态栏是瞬时的：命令失败会被写成错误状态，而接收端每次
  状态推送又会清掉非错误状态，导致提示要么被抹掉、要么把失败的投屏盖成绿色成功。现在它是
  独立的一条信息，错误优先显示。
- 收窄「该网页需要登录」的判定，改为匹配各站点实际使用的措辞（含 Vimeo 的 `logged-in`）。
  此前只要错误输出里出现 `cookies` 字样就归类为需要登录，而该词也出现在与登录无关的通用提示中。
- Cookie 源不可用且页面确实需要登录时，同时说明两件事并建议改用 Firefox，不再只报其中一半；
  重试失败时保留首次的 Cookie 错误诊断，不再被后一次的错误掩盖。
- 接收端诊断日志在回传过程中按快照读取。此前每个分块都会重新读取仍在变化的缓冲区，
  日志写满后取回的内容可能丢行、重复或在多字节字符中间截断。
- 接收端诊断日志在留存和写入 logcat 前脱敏 Bearer token、Cookie、私钥和完整 URL，
  与发送端日志的处理保持一致。
- 超长日志条目按字符边界截断，不再产生半个多字节字符。
- 取回的接收端日志按最新 3 个文件保留，同一秒内的多次取回不再互相覆盖。此前
  `receiver-*.log` 不受发送端日志轮转管理，会无上限堆积。
- 快照已释放时的续读请求返回 `invalid_state`，发送端自动从头重取一次，不再用新快照静默拼接。
- 发送端在整个取回过程中校验 `totalBytes` 不变，逐块重读实时缓冲区的接收端会被识别出来。
- 单个字符长度超过请求的 `maxBytes` 时返回该完整字符，不再返回空的非 EOF 分块导致客户端空转。

### 安全

- 仅在接收端证书与已保存 pin 校验通过后才发送 `trustedToken`。首次信任（TOFU）连接的对端
  身份未经验证，此时不再交出该凭据，避免同网段冒充者套取可信凭据后向真实接收端冒充发送端。
- 恢复 pin 查找对「设备 id / 地址派生手动 id」两个来源的回退，证书固定不再因连接入口不同
  而被跳过。
- 所有未鉴权来源的诊断日志（WSS 握手/帧错误、发现应答失败、会话前解码失败）共享一份有限
  预算，且仅在成功建立会话时恢复，均不记录堆栈。此前未配对的局域网主机可以靠反复重连，
  或用源端口为 0 的发现查询让应答发送失败，把真正的诊断信息挤出有界缓冲。
- 解码失败不再把原始帧片段写入日志。该片段由对端控制，畸形的 `session.hello` 曾可能把
  `trustedToken` 原样留在缓冲里，供之后配对的另一个发送端取走并重放。日志脱敏同时按
  字段名后缀匹配作为兜底——解码器会把帧裁成失败位置附近的窗口，字段名开头常被截断。
- 私钥脱敏覆盖 `RSA`/`EC`/`ENCRYPTED PRIVATE KEY` 等 PEM 头，不再只匹配 `PRIVATE KEY`。
- `maxBytes` 类型错误（如字符串 `"16384"` 或小数）返回 `invalid_message`，不再静默回退到默认值。
- 接收服务自身启动失败（如控制端口被占用）仍完整记录堆栈并在接收画面提示，不受未鉴权
  日志预算影响——否则接收端会静默失效，而诊断日志本身正依赖这条已失效的控制通道回传。
- 重新信任对话框按地址和证书指纹标识接收端，不再仅凭发现响应里未经认证、可被冒充的设备名。

## 1.0.0 - 2026-08-29

首个稳定版本。

### 功能

- Android 接收端与 Windows/Android 发送端。
- 局域网自动发现、手动 IP、连接码配对、证书固定 WSS 和自动重连。
- 本地视频 HTTPS Range 流式投放、持久播放列表和基础播放控制。
- HTTP、HTTPS、HLS、DASH、RTSP 及 Windows 网页视频解析。
- 抖音分享文本 URL 提取、视频解析及 H.264/AAC 分轨播放。
- 照片批量传输、宫格讲解和断线续传。
- 接收端有界磁盘缓存与发送端有界滚动诊断日志。

### 修复与兼容性

- 移除本地媒体客户端总调用超时，避免长视频约 17 秒后停止读取。
- Windows 防火墙规则覆盖 Domain、Private 和 Public 网络。
- 播放列表跨发送端重启恢复，并保留不可用文件提示。
- Windows Setup 支持同路径覆盖安装。

### 已知限制

- 当前版本不包含屏幕镜像。
- Windows 二进制尚未使用 Authenticode 证书签名，首次运行可能触发 SmartScreen。
- UDP 自动发现只覆盖同一广播域；跨子网需要手动 IP 和双向路由。
